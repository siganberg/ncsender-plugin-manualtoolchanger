⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## What's Changed

### 🔧 Improvements
- Tool length sensor approach now uses a probing move instead of a blind rapid move, so the machine stops safely on contact instead of crashing if the sensor is triggered early or positioned unexpectedly
- The macro editor now follows the ncSender dark and light themes, so its colors match the rest of the app when you switch appearance
